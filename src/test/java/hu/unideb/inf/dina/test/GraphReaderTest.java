/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package hu.unideb.inf.dina.test;

/**
 *
 * @author admin
 */
public class GraphReaderTest {
/*
    @Test
    public void CSVGraphReaderTestWithNodeNumber() throws IOException {
        Graph g = new Graph();
        g.readGraphFromFile(new VertexPairGraphReader(), new File("test-graphs/tinyGraph.txt"));
        System.out.println("Graph size: " + g.getVertices().size());
        assertEquals(21, g.getVertices().size());
    }

    @Test
    public void CSVGraphReaderTestWithoutNodeNumber() {
        Graph g = new Graph();
        try {
            g.readGraphFromFile(new VertexPairGraphReader(), new File("test-graphs/tinyGraphNoNodenum.txt"));
        } catch (Exception e) {
            fail();
        }
        System.out.println("Graph size: " + g.getVertices().size());
        assertEquals(21, g.getVertices().size());
   }*/
}
